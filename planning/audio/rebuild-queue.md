# Очередь пересборки корпуса

Исправления конвейера, уже проверенные на отдельных песнях и ждущие плановой пересборки всего корпуса (правило: пересборка — по
накоплении, не на каждое исправление; `docs/rules/audio-alignment.md`).

| дата | исправление | где проверено | кого касается |
|---|---|---|---|
| 2026-09-09 | мягкий конец пения (`SOFT_END`): тихая последняя нота не обрезается | D 399 Хольцмайр | конец каждой записи; после двух ночных пересборок применено ко всем, кроме удержанных, чьи файлы подменились старыми (см. ниже) |
| 2026-09-09 | хвост строки после последнего якоря дописывается по строкам | D 399 Фишер-Дискау | записи, где Whisper не услышал повтор в конце |
| 2026-09-09 | фантомные повторы: проходы из дораспознанных слов и частичные повторы строки — только при сходстве декода CTC с текстом ≥ 0,45; окна дораспознавания не внутри тянущейся гласной | D 39 Рот (точечно выложено) | ~446 подозрительных частичных повторов в 300 записях (`$SP/suspect_repeats.json`) |
| 2026-09-09 | `finish_cloud.sh`: прежний файл удержанной записи удалялся, а не перемещался поверх новой версии | — | удержанные записи ночных пересборок (~280) на сайте — старые версии; обновятся при плановой пересборке |
| 2026-09-09 | D 39 Рот: фильтр фантомов отбросил настоящее «Entstiegen@351.4» → дыра 9 с на 343–352 с | не починено | разобрать отдельно |
| 2026-09-09 | при надёжном своём маршруте (якорей ≥ 70 % его слов) запасной путь идёт по СВОЕМУ маршруту, а не по консенсусу; консенсусная удержанная версия — не кандидат (`finish_control.sh`, `publish_held.py`, `fallback.sh` не подменяет route_consensus.json) | D 5 Мельцер (точечно выложено спасение по своему маршруту) | все записи с решением «запасной (дыры: …)» — ~300 |
| 2026-09-09 | фильтр фантомов: уверенный проход Whisper (p ≥ 0,5, таймкоды в норме) при звучащем голосе — не фантом даже при малой буквенной массе (CTC глух к сопрано); декод-проверка частичных повторов — только для слабых проходов; слабые проходы спасаются декодом | D 23 Яновиц (точечно выложено: повторы «dass ich nicht widerstand» и «Wenn ich…» вернулись, 0 дыр), D 39 Рот без регресса | сопрано и записи с повторами — по всему корпусу |
| 2026-09-09 | нерешено: порядок слов внутри строки у певца иной, чем в тексте (D 23, Яновиц: «Wenn ich ihn von fern seh» при тексте «Wenn ich von fern ihn seh») — сопоставление слова внутри строки не переставляет, «ich» растягивается на «ihn»; нужна поддержка перестановки как варианта строки | D 23 Яновиц | редко; кандидат в сверку текста |
| 2026-09-09 | `fragments_order.py`: устаревшая пометка «фрагмент» у записи, переставшей быть фрагментом, теперь снимается | D 23 | performances.json |
| 2026-09-09 | перестановка слов внутри строки: при уверенных, но не монотонных по времени якорях слова подсвечиваются по якорям Whisper (порядок текста не навязывается); слово строки без якоря ищется среди вставок в окне прохода | D 23 (Мюллер-Брахман, Яновиц) — не сработало: сопоставление уносит переставленное слово в следующую строку; настоящее решение — правка текста (`text-mismatch-confirmed.md`) | редкие случаи |
| 2026-09-09 | перестановка слов внутри строки → варианты в спетом порядке (perm); ручные правки по слуху (`manual/`) накладываются после сборки | D 23 Мюллер-Брахман, Маклафлин (dahin/entflohn), Яновиц (manual) | записи, где певец меняет порядок слов |
| 2026-09-09 | «второе мнение» окном: слова Whisper с p < 0,4 перераспознавать на окне ±3–5 с без контекста записи (Retr на GPU или локально); принимать, если два режима декодирования согласны и результат — слова той же строки; контекст сплошного прохода навязывает порядок текста (D 23, Яновиц: «ihn von fern sehe» p 0,21 → окном «seh' ihn von fern» p 0,74–0,99) | D 23 Яновиц (проверено экспериментом, в конвейер не внесено) | слова с низкой уверенностью по всему корпусу |
