export const meta = {
  name: 'music-facts',
  description: 'Добор музыкальных фактов для страниц без раздела «Музыка» (sonnet; черновики разделов и Fable — отдельным пакетом после сброса пула)',
  phases: [{ title: 'Музыкальные факты' }],
}
// args = {groups:[{id, songs:[...]}]}
const REPO = '/workspaces/schubert-lieder'
const one = (g) => agent(`Ты — исследователь-фактограф проекта «все песни Шуберта с пословным русским переводом». У песен D ${g.songs.join(', D ')} на странице НЕТ раздела «Музыка»: при первом сборе музыкальных сведений не нашлось. Твоя задача — добрать их и записать в файлы фактов. Сами страницы НЕ трогать: разделы будут написаны отдельным проходом.

ПО КАЖДОЙ ПЕСНЕ
1. Определи файл: Bash node -e 'const i=require("${REPO}/app/src/data/index.json");const e=i.find(x=>String(x.d)==="<d>");console.log(e.file, e.title_de, e.year)'; slug — имя файла без .json.
2. Прочитай нынешний файл фактов ${REPO}/planning/research/<slug>-facts.md — что уже есть и что раздел «4. Музыка» отмечает как ненайденное; повторно искать то, что уже подтверждено, не нужно.
3. Посмотри предзагруженные источники: /home/vscode/schubert-waves/*/prefetch/<slug>/ — schubertlied.de.txt (немецкий разбор: тональность, форма, такты), schubertsong.uk.txt (комментарий Малколма Рена), hyperion-booklet-*.txt (аннотация Грэма Джонсона), manifest.json.

ЧТО ИСКАТЬ (это и есть цель)
Тональность (и её смены), размер, темповое обозначение, форма (строфическая / сквозная / с рефреном), фигура аккомпанемента, ключевые музыкальные события по строфам, число тактов, опус и первое издание, посвящение, известные трудности исполнения, редакции песни.

ГДЕ ИСКАТЬ СВЕРХ КЭША (бюджет сети — до 15 обращений на песню, Bash + curl)
— **IMSLP**: https://imslp.org/wiki/<Название>,_D.<номер>_(Schubert,_Franz) — карточка произведения даёт тональность, темповое указание, опус, издателя и год первого издания; страницу снимать так: curl -sL '<url>' | sed 's/<[^>]*>/ /g' | tr -s ' \\n'.
— **Буклеты Hyperion**: https://www.hyperion-records.co.uk/notes/CDJ<номер>-B.pdf (номера томов — в ${REPO}/planning/research/<slug>-top5.md и в манифесте); разбирать pdftotext -layout, искать окно вокруг «D <номер>».
— **Немецкая Википедия** — статьи о песнях Шуберта опираются на Дюрра и Файля (в проекте она принята как их конденсат); **schubert-online.at**; предисловия Новой шубертовской редакции (Bärenreiter).
— Форумы, блоги без авторства и ИИ-сводки источниками НЕ считаются.

КАК ЗАПИСАТЬ
В файл ${REPO}/planning/research/<slug>-facts.md, раздел «4. Музыка», добавь факты следующими свободными номерами в формате файла: «**Ф№.** атомарное утверждение — короткое имя источника — «дословная цитата ≤120 знаков» — **verified|secondary|uncertain**»; URL нового источника — один раз в шапку файла в список «Источники». Чего найти не удалось — честно допиши в раздел «Не подтвердилось / не найдено». Ничего из памяти: нет цитаты из реально загруженного источника — нет факта.

ЗАПРЕТЫ: файлы песен в app/src/data/songs НЕ менять; страницы не писать; чужие песни не трогать; субагентов не спавнить; временные файлы — только в /tmp/claude-1000/-workspaces-schubert-lieder/6dd19060-2ba3-4979-b2ed-3349f9729473/scratchpad.

Верни по схеме: group="${g.id}", songs_done, facts_added, songs_still_empty (по скольким песням музыкальных сведений так и нет), notes (до 300 знаков: что сработало, что нет), tool_calls.`, {
  label: `музыка ${g.id}`, phase: 'Музыкальные факты', model: 'sonnet', effort: 'high',
  schema: { type: 'object', properties: { group: { type: 'string' }, songs_done: { type: 'number' }, facts_added: { type: 'number' }, songs_still_empty: { type: 'number' }, notes: { type: 'string', maxLength: 300 }, tool_calls: { type: 'number' } }, required: ['group', 'songs_done', 'facts_added', 'tool_calls'] },
})
const res = await parallel(args.groups.map((g) => () => one(g)))
return { res }
