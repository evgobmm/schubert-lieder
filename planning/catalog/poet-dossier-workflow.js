export const meta = {
  name: 'poet-dossiers',
  description: 'Досье поэтов корпуса Шуберта (по одному агенту sonnet на поэта): факты с цитатами и URL → planning/research/poets/<slug>.md',
  phases: [{ title: 'Досье' }],
}
// args = {poets: [{slug, name}]}
const REPO = '/workspaces/schubert-lieder'
const one = (p) => agent(`Ты — исследователь-биограф проекта «все песни Шуберта с пословным русским переводом» (сайт с пословным русским подстрочником и разделом «О песне» к каждой песне). Твоя задача — ОДНО досье поэта: ${p.name} (файл ${REPO}/planning/research/poets/${p.slug}.md).

ПОРЯДОК РАБОТЫ
1. Bash: node -e 'const i=require("${REPO}/app/src/data/index.json");console.log(i.filter(e=>(e.poet_de||"").includes("${p.grep}")).map(e=>"D "+e.d+" «"+e.title_de+"» "+(e.year||"")).join("\\n"))' — список песен корпуса на этого поэта.
2. Read: ${REPO}/planning/research/poets/collin.md — ЭТАЛОН структуры, тона и оформления фактов (не копируй содержание, только форму).
3. Read: ${REPO}/docs/rules/style.md — язык обязателен по этому своду (без ИИ-артефактов, «ё» везде).
4. Сбор источников. Приоритет: deutsche-biographie.de (ADB/NDB), geschichtewiki.wien.gv.at, буклеты Hyperion (hyperion-records.co.uk/dc.asp и dw.asp — комментарий Грэма Джонсона о поэте и о песнях Шуберта на его тексты), schubertlied.de, lieder.net, de.wikisource.org, de.wikipedia.org (только как навигация к первичным данным, не как источник фактов). Инструменты: WebSearch/WebFetch, если доступны, иначе Bash curl (curl -sL '<url>' | sed 's/<[^>]*>/ /g' | tr -s ' \\n' | head -c 20000) и поиск через curl -sL 'https://html.duckduckgo.com/html/?q=<запрос+через+плюсы>' | grep -o 'https://[^"<]*' | head -20. Бюджет сети — до 20 обращений.
5. Напиши файл ОДНИМ вызовом Write.

ФОРМАТ ФАЙЛА (как в collin.md)
# Русское имя (Original Name, годы жизни)
Строка «Песни корпуса: D … «…», … (по \`app/src/data/index.json\`).» — если песен больше 15, перечисли числом и назови самые известные.
## Кратко (заготовка для «О песне») — один-два плотных абзаца: кто это для читателя песни, чем важен для Шуберта. Это текст, который переводчики будут переиспользовать почти дословно, поэтому он должен быть написан начисто.
## Биография (по фактам) — 1–3 абзаца, каждое утверждение со ссылкой (Ф№).
## Шуберт и поэт — как тексты попали к Шуберту, сколько песен, известные датировки, посвящения, свидетельства современников; со ссылками (Ф№).
## Стиль и место в литературе — со ссылками (Ф№).
## Факты — нумерованный список: «**Ф№.** атомарное утверждение. — <URL> — «дословная цитата из источника» — **verified**» (verified — два независимых источника или первичный документ; secondary — один вторичный; uncertain — расхождение или предположение, с описанием расхождения). 25–40 фактов, обязательно: даты и места рождения/смерти, образование и служба, ключевые издания стихов (год, издатель), связь с Шубертом, число песен, оценка стиля.

ЗАКОНЫ
— Ничего из памяти: нет цитаты из реально прочитанного источника — нет факта (или uncertain с явной пометкой «не подтверждено»).
— Цитаты дословные, ≤200 знаков; у каждого факта — рабочий URL.
— Проза досье — по-русски, живо и точно, без ИИ-артефактов («не X, а Y» как украшение, «не просто», канцелярит), имена людей с ролью при первом упоминании, немецкие названия давать с русским переводом в кавычках при первом упоминании.
— Расхождения источников не сглаживать, а называть.
— Пиши ТОЛЬКО свой файл ${REPO}/planning/research/poets/${p.slug}.md; никакие другие файлы репозитория не меняй; субагентов не спавнить.
Верни по схеме: slug="${p.slug}", ok, n_facts, songs (сколько песен корпуса у поэта), tool_calls.`, {
  label: `досье ${p.slug}`, phase: 'Досье', model: 'sonnet', effort: 'high',
  schema: { type: 'object', properties: { slug: { type: 'string' }, ok: { type: 'boolean' }, n_facts: { type: 'number' }, songs: { type: 'number' }, tool_calls: { type: 'number' } }, required: ['slug', 'ok', 'n_facts', 'tool_calls'] },
})
const res = await parallel(args.poets.map((p) => () => one(p)))
return { res }
