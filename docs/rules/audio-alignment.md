# Синхронизация слов с записью (пословные тайминги)

Цель — подсвечивать пропеваемое слово во время проигрывания записи на странице песни.
Указание пользователя (2026-09-07): распознавание с таймингами делать **как можно дешевле**,
потому что масштаб — до ~628 песен × 5 записей (~3100 дорожек, ~180 часов звука).

## Главное решение: это не распознавание, а выравнивание

Немецкий текст песни у нас уже есть и он выверен. Значит задача — **не ASR (распознать, что поют),
а forced alignment (принудительно уложить известный текст на звук)**. Это на порядок дешевле,
точнее и не зависит от того, насколько плохо ASR слышит оперное пение.

Отсюда следует запрет: **не платить за облачные ASR-API** (Deepgram/AssemblyAI/Google STT и т. п.)
ради таймкодов. На нашем объёме это $45–165 и худшее качество, тогда как локальное выравнивание
стоит ноль, а на арендованной GPU — единицы долларов за весь корпус.

## Конвейер (проверен на пилоте D 118, 2026-09-07)

1. `yt-dlp` — скачать дорожку конкретного `videoId` из `performances.json`; `ffmpeg` → 16 кГц моно WAV.
2. **Demucs** (`htdemucs --two-stems=vocals`) — отделить голос от фортепиано. Шаг обязательный:
   без него акустическая модель на камерной фактуре Шуберта заметно хуже (см. замеры ниже).
3. **Два акустических движка CTC** по вокальной дорожке, оба обязательны:
   основной — `torchaudio.pipelines.MMS_FA` (модель, обученная именно для выравнивания; границы слов
   у неё заметно точнее), второй — `jonatasgrosman/wav2vec2-large-xlsr-53-german` (немецкая ASR-модель;
   увереннее в буквах, но границы хуже). Длинный звук резать на окна 20 с с полями по 2 с.
   ПЕРЕПИСАНО 2026-09-07: было «немецкая — заметно лучше MMS_FA, значения берём у неё». Причина:
   уверенность в буквах (`score`) и точность границ — разные вещи; на девяти местах, найденных
   пользователем на слух, немецкая модель попала в 2 проверках из 12, MMS — в 9.
4. **Выравнивание — только в окнах вокальных фраз, не по всей дорожке разом.** Границы фраз берутся
   по огибающей вокального стема (участки громче 25 % от медианной громкости слова, разделённые
   тишиной). Строки текста раскладываются по фразам, и каждая группа строк выравнивается внутри
   своего окна. Без этого ограничения выравниватель свободно переносит целую строку через паузу в
   соседнюю фразу — и результат выглядит безупречно: последовательность монотонна, дыр нет, оценки
   обычные. Так на пилоте строфа `2:0` оказалась запущена на 2.2 с раньше вступления голоса,
   и ни один автоматический признак этого не показал (нашлось расхождением двух движков и ухом).
   Слова к фразам привязываются по грубому выравниванию всей дорожки, причём **по арбитрованным
   позициям двух движков** (см. шаг 5), а не по одному: одна модель, ошибившись, загоняет в неверную
   фразу и вторую. Само выравнивание: `torchaudio.functional.forced_align` + `merge_tokens` → интервалы токенов → слова.
   Нормализация слова под словарь модели: нижний регистр, умляуты по словарю (в немецкой модели
   `ä ö ü` есть), пунктуация снимается.
5. **Выбор движка для каждого слова — динамическим программированием.** Там, где два оконных
   выравнивания расходятся больше 0.3 с, кандидат оценивается по звуку: стоит ли на сильной атаке
   голоса (±0.2 с), не в провале ли громкости, не оставляет ли «пустой» голос в начале фразы;
   у основного движка небольшой бонус. ДП выбирает движок по слову так, чтобы **порядок начал был
   строго возрастающим** — это единственная схема, которая одновременно не переставляет слова местами
   и умеет смешивать движки внутри одной строки (на пилоте: первые шесть слов рефрена у немецкой,
   `Und nimmermehr` после вдоха — у MMS). Выбирать блоками или по слову без ограничения порядка
   нельзя — оба варианта проверены и ломаются.
6. **Начала слов — из атак голоса, а не из CTC.** CTC «выстреливает» буквой где-то внутри её звучания,
   а на громкой тянущейся ноте не узнаёт букв вовсе и ставит слово на спад ноты. Поэтому начало
   поправляется по огибающей вокального стема: (а) начало в тишине — к ближайшему вступлению голоса;
   (б) начало в провале громкости (<50 % опорной), впереди до 0.6 с атака, после которой вдвое
   громче — к атаке, но только если начало не стоит уже на своей атаке (слова с глухой согласной
   и на *pp* начинаются тихо законно); (в) начало на спаде, а между концом предыдущего слова и
   началом была сильная атака — назад к ней.
7. **Продление конца слова по огибающей вокала** — обязательный шаг. CTC ставит конец слова там,
   где модель перестаёт узнавать фонему, а певец держит ноту дальше; на распевах слово обрывается
   на секунду-полторы раньше, чем звучит. Конец каждого слова тянется по огибающей вокального стема
   (кадр 10 мс, порог 20 % от медианной громкости слова, 0.12 с тишины подряд = слово кончилось)
   до реального смолкания голоса или до начала следующего слова.
   Конец не заходит дальше начала следующего слова и конца своей фразы.
8. На выходе — на каждое слово `{i, w, start, end}` плюс отметка, у какого движка взято.

## Повторы живут в таймингах, а не в тексте (указание пользователя 2026-09-07)

Граница проходит **по происхождению повтора** (решение пользователя 2026-09-07:
«всё что у поэта — оставляем, это повторы канонические»):

- **повтор есть у поэта** (рефрен стихотворения, авторская анафора) — остаётся в немецком тексте
  как есть, сколько бы строк ни занимал. Читатель должен видеть стихотворение таким, каким оно
  написано. Пример: три полных рефрена в D 118 стоят у Гёте на тех же местах, это не Шуберт;
- **повтор добавил композитор или певец, длиной в строку и больше** — в тексте его быть не должно,
  он выражается **маршрутом исполнения**. В «Зимнем пути» такие повторы сплошь и рядом;
- **повтор добавил композитор, но длиной в несколько слов** — остаётся в тексте: отдельного
  механизма он не требует и читается естественно. Пример: удвоение «ich finde» в рефренах D 118 —
  это Шуберт, у Гёте слово стоит один раз, но в текст удвоение входит.

Особый случай: композитор может не просто повторить строку, а **переписать её слова**. В D 118
Шуберт, повторяя финальную строфу, заменил её первую строку — это единственные слова песни,
сочинённые им самим, у Гёте им соответствия нет. Такая строка не может быть ссылкой на существующую
строку текста: она хранится отдельным элементом файла таймингов (`extra_lines`), а маршрут
ссылается на неё по идентификатору.

Практическое следствие: перед выравниванием песни надо по источнику установить, где кончается
поэт и начинается Шуберт. Делать это по памяти запрещено — работает `verification-protocol.md`.
Для D 118 это уже сделано: `planning/research/gretchen-text.md`, раздел «Повторы» — первое издание
(Cappi und Diabelli, C. et D. No. 767), AGA (Serie XX № 31) и Гёте 1808 по Deutsches Textarchiv.

Открытый вопрос: к строкам, которые уходят из текста в маршрут, бывают привязаны пояснения
(в D 118 на них висят два комментария — про единственные собственные слова Шуберта и про коду).
Куда девать такие пояснения при чистке текста — не решено.

ПЕРЕПИСАНО трижды за 2026-09-07.
Было (первая редакция): «`stanzas[].lines_de` должны содержать шубертовские повторы строк и слов,
а не голый текст стихотворения; для `text_only` повторы надо восстановить по нотам».
Было (вторая редакция): «немецкий текст повторов НЕ содержит, каждая строфа и строка записаны
один раз». Было (третья редакция): граница по объёму — «повтор в несколько слов остаётся в тексте,
повтор в несколько строк и больше — только в маршруте». Стало: граница по происхождению —
авторское остаётся в тексте целиком, композиторское уходит в маршрут; см. выше.
Повторность крупных кусков несёт маршрут, плеер проходит по одной и той же строфе несколько раз,
подсвечивая те же слова заново.

Следствие: **тайминги — это не плоский список слов по порядку текста**, а последовательность
проходов, каждый со ссылкой на строку текста:

```
route: [ {s: 0, l: 0, w: [[start,end], ...]},   // строфа 0, строка 0
         {s: 0, l: 1, w: [...]},
         {s: 0, l: 0, w: [...]},                 // та же строка спета второй раз
         ... ]
```

Одна и та же пара `(s, l)` встречается в маршруте столько раз, сколько раз она спета.
Порядок в маршруте — порядок звучания.

Отдельная задача (не сделана): в `d118-gretchen-am-spinnrade.json` повторы сейчас вписаны прямо в
текст — их надо вычистить, отделив гётевское от шубертовского. Так же проверить все уже
опубликованные песни.

## Ловить несоответствие текста и записи (указание пользователя 2026-09-07)

Текст в файле может не совпадать с тем, что реально спето: другая редакция, пропущенная строфа,
повторы певца, купюры. **Это надо обнаруживать автоматически, а не натыкаться на слух.**

Отсюда главный принцип: **маршрут исполнения выводится из звука, а не задаётся руками.**
Порядок шагов:

1. Свободный CTC-декод (без текста) по вокальному стему — грубая фонетическая расшифровка.
   На отделённом вокале она уже читается: в пилоте по первым строкам D 118 узнаются реальные слова.
2. Сопоставить эту расшифровку со строками текста (поиск последовательности строк, наилучшим
   образом покрывающей расшифровку, по расстоянию редактирования на уровне символов).
   Результат — сам маршрут: какие строки, в каком порядке, сколько раз.
3. По развёрнутому маршруту сделать forced alignment и получить тайминги.

Этот же шаг 2 и есть детектор: строка текста, не нашедшая себе места в звуке (или кусок звука,
которому не нашлось текста) — прямой сигнал расхождения. Такая песня помечается и не публикуется
с таймингами до разбора.

Дополнительные (более слабые) сигналы: аномально длинные слова и разрывы внутри фразы; резко
низкая оценка по отдельной строке на фоне остальных.

## Контроль качества (обязателен)

Оценка CTC (`score`) сама по себе низкая на пении и плохо отделяет верное от неверного. Рабочий
сигнал — **расхождение двух независимых прогонов** (разные модели и/или микс против вокала):
где прогоны сходятся в пределах 0.3 с — доверяем, где расходятся — слово помечается и правится руками.
На пилоте это сразу поймало реальную ошибку: `MMS_FA` на строфах «Sein hoher Gang…» уехал на 10–16 с,
а два немецких прогона там сошлись между собой — то есть ошиблась именно MMS.

Проверка на слух — обязательная приёмка: локальная страница с подсветкой слова под звук
(звук на выбор — оригинал или вокальный стем, разметка на выбор — любой из прогонов,
расхождения подчёркнуты). Только после неё тайминги идут в репозиторий.

## Замеры пилота (D 118, Sampson — Middleton 2018, 225 с, 166 слов, 16 ядер CPU)

| Прогон | median score | вывод |
|---|---|---|
| MMS_FA по миксу | 0.083 | уехал на 10–16 с в двух строфах |
| DE-модель по миксу | 0.248 | годен |
| DE-модель по вокалу Demucs | 0.290 | буквы уверенно, границы плохо: 2 из 12 проверок на слух |
| **MMS_FA по вокалу Demucs** | 0.167 | **основной движок**: 9 из 12 проверок на слух |
| объединённая эмиссия DE+MMS | 0.107 | 8 из 12 — хуже MMS, отвергнута |
| фонемная `wav2vec2-lv-60-espeak-cv-ft` по вокалу | 0.007 | **непригодна, отвергнута** |

Скорость на CPU: wav2vec2-large — 126 с на 225 с звука (≈1.8× быстрее реального времени),
Demucs — 4 мин 41 с на 3,5-минутную запись при 48 кГц стерео (≈1.3× медленнее реального времени),
то есть ~6 минут процессора на дорожку, Demucs — главная статья.

**Хост devcontainer — AMD Ryzen 9 5900HX (8 ядер/16 потоков) со встроенной Radeon** (2026-09-07);
пользователь считает машину ASUS Zenbook 14, но такой чип там не ставили — это игровой ноутбук или
мини-ПК. Есть ли отдельная NVIDIA — неизвестно, проверяет пользователь (Диспетчер задач → GPU или
`nvidia-smi` в PowerShell). Встроенная графика для PyTorch/CUDA бесполезна; если NVIDIA есть —
проброс в контейнер (`"runArgs": ["--gpus","all"]`, CUDA-сборка torch) даёт бесплатный быстрый путь. Значит на корпусе выбор такой: либо фоновый счёт на CPU
(~60 часов на одну запись к каждой из 600 песен), либо аренда GPU — почасовая (RunPod/Vast.ai,
~$2–5 за весь корпус) или бессерверная (Modal, бесплатного кредита, по оценке, хватает). Звук при
этом скачивать из devcontainer, а не с арендованной машины: YouTube блокирует адреса дата-центров.

### Что проверено и чего делать не надо

- **Разделение источников — причина грубых промахов, а не мелкая оптимизация.** MMS по миксу
  расходилась с эталоном до 16 с; она же по вокальному стему — уже 75 % слов в пределах 0.1 с,
  худшее расхождение 4.7 с. То есть катастрофу давал не выбор модели, а фортепиано в сигнале.
- **Фонемное выравнивание не работает** на этом материале: espeak-фонемизация немецкого +
  `facebook/wav2vec2-lv-60-espeak-cv-ft` дали расхождение с эталоном 10 с по медиане.
  Низкая оценка (0.007 против 0.29) поймала это ещё до сравнения — значит `score` годится как
  грубый фильтр «прогон вообще состоялся или нет», хотя внутри годного прогона она слабая.
- **Глобального сдвига нет, подгонять его не надо.** Сверка начал слов с онсетами вокального стема
  (спектральный поток, шаг 5 мс): медиана +20 мс, среднее +14 мс.
- **Структура сходится с нотами.** Все длинные паузы разметки приходятся на границы строф, то есть
  на проигрыши; самая длинная (12.9 с перед третьим рефреном) — та самая фермата, где фигурация
  прялки останавливается и начинается заново (`gretchen-text.md`).
- **Мельче 20 мс кадра смысла нет** — для слова это ниже порога заметности.

### Что дало продление конца слова (D 118, 2026-09-07)

Дефект был слышен как «подсветка сваливается со слова раньше времени». Замер по вокальному стему:
из 20 пауз внутри строк в 18 голос продолжал звучать, на кульминациях — втрое-вдесятеро громче
средней громкости слова. То есть тянущиеся ноты не были размечены вообще.

После продления: пауз внутри строки длиннее 0.35 с осталась одна, и там голос действительно тих
(28 % — вдох). Пауз длиннее 1 с между словами осталось пять, все с громкостью 4–8 % от опорной,
то есть все они — настоящие фортепианные проигрыши. Медианная длительность слова 0.56 → 0.88 с;
патологические слова короче 0.05 с исчезли сами.

### Начала в тишине (D 118, 2026-09-07)

Отдельный дефект, замеченный пользователем на слух в третьей строфе: слово было поставлено ровно
в паузу между фразами и держало подсветку 4.1 с, пробивая тишину насквозь. По корпусу песни таких
слов нашлось 7, но существенное только одно (сдвиг 0.72 с), остальные — косметика 0.10–0.18 с.
То есть начала слов в целом ложатся на голос, и это подтверждает качество выравнивания.

### Порог склейки подсветки

После того как концы слов продлены до реального смолкания голоса, все оставшиеся паузы между
словами — настоящая тишина. Значит `GAP_BRIDGE = 1.0` в `app/src/utils/timings.js` стал слишком
велик: он заставляет предыдущее слово держать подсветку через паузу до секунды, тогда как раньше
этот порог компенсировал недотянутые концы. Разумное значение — около 0.3 с (уровень вдоха).
Не изменено: правка в чужом коде, решение за пользователем.

### Второй движок — соавтор через ДП, а не контролёр

ПЕРЕПИСАНО 2026-09-07. Было: «значения берём у DE, MMS только помечает подозрительные слова;
усреднять не надо». Стало: значения берутся у **того движка, который в этом слове стоит на звуке**
(шаг 5 конвейера), и это решает ДП с жёстким порядком. Усреднять по-прежнему нельзя — медиана из
двух тянет верное значение к неверному; выбирается одно из двух.

Замер по девяти местам, найденным пользователем на слух (плюс проверки «не сломать соседей»):
немецкая одна — 2/12; MMS одна — 9/12; MMS + привязка к атакам — 11/12; оконное выравнивание обоих
движков + ДП + атаки — **14/15**, единственный недобор 66.19 против порога 66.2 (то есть попадание).
Список расхождений двух движков (29 слов из 166) остаётся полезен как **очередь приёмки на слух**.

Чего автоматика не ловит: сдвиг целой строки через паузу в соседнюю фразу выглядит правдоподобно по
всем числам — его нашёл пользователь ухом, и только после этого появились окна фраз. Приёмка на слух
обязательна и после этого конвейера.

## Правки — только точечные; обсуждённые песни заморожены (переписано 2026-09-08)

Пользователь: «остальное очень неплохо, не хотелось бы, чтобы менялось (чтобы не было „исправили
в 2 местах, сломали в остальных“)». Отсюда правила:

- **Разметка, принятая пользователем на слух, не пересчитывается целиком.** Любая правка вносится
  только в названные им строки. Перед записью файла скрипт сравнивает новую версию с принятой и
  падает, если изменилось начало хоть одного слова вне этих строк (подрезка *конца* соседнего слова
  до нового начала допустима — на подсветку она не влияет).
- **Принятый файл D 118 — регрессионный тест конвейера.** Изменение алгоритма считается допустимым,
  только если на D 118 оно воспроизводит принятые начала (допуск 0.15 с) везде, кроме мест, которые
  пользователь сам назвал плохими. Улучшение «в среднем» ценой перестановок в принятых местах не
  принимается.
- Урок дня: сборка через ДП исправила девять названных мест и одновременно сломала два других
  (`mir` в 3-й строфе, `So wie ich` в предпоследней) — оба были верны в предыдущей версии. Таблицу
  «до → после» надо строить **по всем 166 словам**, а не только по названным местам, и смотреть
  каждое смещение больше 0.3 с.

### Нерешено: арбитр не отличает свою атаку от чужой

Пословная оценка кандидата (стоит ли на атаке, не в провале ли) не видит, *чья* это атака:
позиция «`mir` на 42.5» стоит ровно на атаке следующего слова и набирает очки, хотя два слова при
этом втиснуты в одну ноту, а 1.6 с голоса перед ними остались без начала слова. Бонус за громкость
не при чём — без него ошибки повторяются. Нужен парный член: штраф за сильную атаку, оставшуюся без
начала слова между двумя соседними началами (но мягкий — на распевах ноты внутри слова законны).
Пока не сделано; на D 118 обе строки возвращены к принятым значениям вручную.

## Все пять записей D 118 (указание пользователя 2026-09-07)

«Сейчас хорошее качество. Мне нужно такое же качество в 4 оставшихся видео» — разметка Сэмпсон
принята и становится **эталоном качества и шаблоном**: у остальных записей тот же текст и та же
партитура, поэтому принятые границы слов переносятся на новую запись через DTW по хроме микса
(с перебором 12 транспозиций — певцы поют в разных тональностях) и служат подсказкой «где слово
должно быть». Движки дают точное «когда», шаблон — грубое «где»; кандидат, ушедший от шаблона
дальше ~0.15 с, штрафуется. Каждая новая запись выдаётся с **очередью прослушивания**: слова, где
итог расходится с шаблоном или движки — между собой.

### Результат на четырёх записях (2026-09-07)

Конвейер с шаблоном (`planning/audio/scripts/pipeline2.py`) прошёл все четыре записи без единого
нарушения целостности; отклонение начал от перенесённого шаблона — медиана 0.06–0.07 с у всех.
Источники начал: MMS 120–143 слов, немецкая 11–18, сам шаблон 12–28 — больше всего у Шуман 1936
(шумный стем, движки слабее — шаблон подстраховал). DTW: Людвиг поёт на 2 полутона ниже и на 13 %
быстрее Сэмпсон, Бонни на 19 % медленнее; стоимость пути 0.011–0.022. Введена минимальная
длительность слова 0.12 с (за счёт длинного соседа): десять коротких безударных слов были зажаты
до 0–0.08 с. Очереди прослушивания (отклонение от шаблона >0.3 с или длительность <0.15 с):
Бонни 4, Шварцкопф 13, Людвиг 9, Шуман 16 слов. На слух не проверено — `verified_by_ear: false`.

Урок по DTW: без ограничения наклона путь вырождается (шаблон 224 с уложился в 22 с записи Людвиг);
шаги 1:1, 1:2, 2:1 обязательны, заодно векторизованный вариант считает за секунды вместо двух минут.

## Вторая песня — «Rückblick» D 911/8, все пять записей (указание пользователя 2026-09-07)

После приёмки четырёх записей D 118 («отработало хорошо») — следующая песня: D 911/8, все пять
записей из `performances.json`. Отличия от D 118:

- ПЕРЕПИСАНО 2026-09-07 (вечер). Было: «принятой записи ещё нет, поэтому шаблон берётся
  предварительный — главная запись без шаблона, остальные четыре с ней как шаблоном». Стало: **все
  пять записей — без шаблона**, как главная. Причина: пользователь услышал, что Quasthoff «очень
  неплохо», а Маттеи «намного хуже»; проверка показала, что перенос по хроме на финальной строфе,
  спетой дважды на ту же музыку, сдвигает разметку на целую строку (3.4 с), а сильный вес шаблона
  заставляет ДП за ним следовать: у Маттеи треть начал, у Фишер-Дискау половина были взяты из шаблона,
  не из звука. Слабый вес не спасает. Правило: **доля начал, взятых у шаблона, выше ~20 % — красный
  флаг**, шаблон отключать; на песнях с повторами целых строф на ту же музыку шаблон по хроме не
  применять вовсе. Шаблон остаётся допустим там, где он проверен (D 118: принятая Сэмпсон, доля 7–17 %).
- **Повторы Шуберта**: в «Зимнем пути» он повторяет строки; по правилу о повторах они не входят
  в текст, а живут в маршруте. Что именно спето — устанавливается по источнику или по звуку
  (свободный декод по вокалу), не по памяти; для каждой строки-повтора в маршруте отдельный проход.
- Имя файла таймингов для D с дробью: `d911-8-<videoId>.json` (`word-sync.md`).

### Уроки D 911/8 (2026-09-07)

- **DTW переноса шаблона — только с якорями на начало и конец пения** (по вокальным стемам обеих записей)
  и с фиксированными концами. Со свободным концом путь обрывается досрочно, когда финальная строфа спета
  дважды на ту же музыку: алгоритму дешевле уложить второе повторение шаблона на первое повторение записи.
  На четырёх записях Rückblick путь заканчивался за 15–35 с до конца пения, и финал сжимался в кучу.
  На D 118 это не проявилось только потому, что там нет таких повторов подряд.
- **Маршрут из звука: работает декод по фразам, не работают ни «непокрытые фразы», ни правдоподобие
  гипотез.** Голое выравнивание не оставляет дыр, а растягивает слова на повтор; суммарное правдоподобие
  растёт от любого добавленного текста у ASR-модели и непоследовательно у MMS. Надёжно: разбить вокал на
  фразы, снять жадный декод каждой и прочитать последовательность строк; согласие независимых записей —
  подтверждение. Найдено так: повтор двух последних строк во 2-й и 4-й строфах, финальная строфа дважды
  плюс две последние строки и последняя ещё раз. Автоматизация сопоставления декода со строками — TODO.
- **Слова без букв** (тире отдельным словом) есть в текстах; для сайта это слово, интервал ему нужен —
  нулевой, у конца предыдущего слова. Все выравниватели должны это переживать.
- Пять файлов D 911/8 выложены с `verified_by_ear: false`; четыре вторичных наследуют ошибки главной
  (Quasthoff) до её приёмки — после приёмки пересчитать (`run_r8_others.sh`, минуты).

## Шаблон не нужен: независимые разметки и поиск выбросов (указание пользователя 2026-09-07, вечер)

Пользователь: «я не уверен, что вообще нужен шаблон. Можно смотреть все и исследовать только выбросы.
Я же не буду вручную проверять эталон». ПЕРЕПИСАНО. Было: принятая на слух разметка одной записи
переносится DTW на остальные и служит кандидатом и якорем. Стало: **каждая запись выравнивается
независимо, одной и той же конфигурацией без шаблона**; несколько записей одной песни сравниваются
между собой (после DTW-сопоставления времени — только для сопоставления, не для переноса позиций):
слово, у которого одна запись расходится с остальными по относительному положению внутри фразы, —
**выброс**, и проверять на слух надо только выбросы. Эталон руками не проверяется. Опыт D 911/8
подтвердил: без шаблона четыре записи получились не хуже главной, а с шаблоном — хуже.

### Ещё два урока D 911/8 (2026-09-07, поздний вечер)

- **Слова без букв не подсвечиваются** (пользователь: «отдельно подсвечивать тире — это абсурд!»). Для сайта
  тире, стоящее отдельно, — слово по пробелам, интервал ему нужен; ставится **нулевой интервал в точке
  начала следующего слова**: окно подсветки пусто, предыдущее слово держится до следующего. Прежняя
  запись «нулевой интервал у конца предыдущего слова» была ошибкой — склейка пауз растягивала подсветку
  тире до следующего слова.
- **Привязка слов к вокальным фразам — по декоду, а не по грубому выравниванию** (`pipeline.py`).
  Когда одна строка спета трижды подряд (конец Rückblick), грубое выравнивание ставит границы между
  повторами произвольно: у Фишер-Дискау последняя строка ушла в шумовой хвост на 7 с, а настоящая фраза
  осталась без слов. Теперь слова раскладываются по фразам монотонным ДП по расстоянию редактирования
  между жадным декодом фразы (немецкая модель) и словами; фразы, где ни один движок не слышит букв,
  исключаются (шум, фортепиано). Стоимость 0.22–0.44 на букву на пяти записях; при стоимости выше 0.7 —
  откат к грубой привязке. Это и есть автоматизация «маршрута из звука», которая раньше была TODO:
  по тем же декодам определяется и порядок строк.

## Третья песня — «Erstarrung» D 911/4 (указание пользователя 2026-09-07, ночь)

«Делаем максимально эффективно: качественно, быстро, без лишнего. Разные люди поют разный текст — это надо
учитывать, и ты должен уметь сам определять это по аудио». Отсюда:
- конвейер — только проверенная короткая цепочка без шаблона и без DTW;
- **маршрут исполнения определяется по декоду для каждой записи отдельно** (певцы могут по-разному
  повторять) и записывается в файл таймингов записи; текст песни не меняется;
- **словесные варианты** (другая редакция, другое слово) детектируются автоматически: слово, где оба
  движка слышат другое, помечается; варианты записываются в файл таймингов (`variants`), интервал
  остаётся у слова текста — подсветка не страдает, а расхождение зафиксировано для редактора.

### Итог D 911/4 (2026-09-08): маршрут и варианты — автоматически

- **Маршрут по декоду для каждой записи** (`route_detect.py`): ДП по вокальным фразам, каждая фраза получает
  непрерывный отрезок слов; следующий отрезок либо продолжает предыдущий, либо начинается с начала уже спетой
  строки (повтор, штраф 4); повтор допускается и внутри фразы (два отрезка); фраза без букв в декоде исключается,
  «пустая» фраза с буквами стоит дорого (2×букв+3). Регрессия на Rückblick: Quasthoff, Mattei, Anders — ручной
  маршрут воспроизведён полностью (31 проход), Fischer-Dieskau и Hotter теряют последний тихий повтор строки
  (в декоде 2–3 звука) — известное ограничение, для подсветки почти незаметное (та же строка).
- **Консенсус между записями** (`assign_cost.py`): собственные маршруты записей расходятся на шумном декоде;
  кандидат по большинству принимается, если объясняет декод каждой записи не хуже собственного (D 911/4:
  везде не хуже, у Андерса лучше). Собственный маршрут записи остаётся, только если он существенно лучше —
  так и учитывается «разные люди поют по-разному», не доверяя шуму.
- **Словесные варианты** (`variants.py`): слово помечается, если оба движка в его интервале слышат одно и то же
  другое слово (согласие декодов ≤0.4, расстояние до текста >0.5, длительность ≥0.25 с) и остальная строка
  слышна хорошо (медиана расстояния по строке ≤0.45). Без этих фильтров — 16 ложных срабатываний на быстром
  пассаже; с ними — 0–2 на запись, и это оказались сдвиги границ слов, а не варианты текста: детектор заодно
  ловит промахи выравнивания. На D 911/4 никто из пяти не отступает от текста сайта на уровне слова.
- Стоимость всей песни (5 записей): ~35 минут фонового счёта (Demucs и эмиссии) плюс секунды на маршрут,
  консенсус и выравнивание; токенов сессии — минимум, работа скриптовая.

### Уроки замечаний пользователя по D 911/4 (2026-09-08)

Пользователь: «неужели невозможно распознать, какие в аудио слова, и на этой основе делать выводы?» — возможно, и
декоды это показывали; отбрасывали мои пороги. Два исправления:
- **Детектор маршрута умеет прыгать вперёд** (к началу строки, штраф 2 + 0.6 за строку дальности; возврат —
  2.5 + 0.6 за строку). Без этого после повтора строк 1–2 он не мог перейти к новой строфе и «пришивал» повтор к
  чужой строфе. Регрессия Rückblick после правки — 4/5 полных совпадений (было 3/5).
- **Порог словесного варианта — 0.25 при согласии движков ≤0.25**, слова от 5 букв, усечения CTC (потерян
  первый/последний звук) не считаются. Варианты редакций близки по буквам (erfroren/erstorben = 0.33) — порог 0.5
  их пропускал. Срабатывание детектора, в котором слышно *начало другой строки* (у Quasthoff «wo find ich» в
  интервале слова `Rasen`), — это не вариант, а **пропущенный повтор в маршруте**: проверять маршрут.
- Файл таймингов записи несёт `variants: [{s,l,k,w,heard_de,heard_mms,start}]` — редактору видно, где певец
  отступает от текста сайта; интервал остаётся у слова текста.

## Распознавание — первично; композиторские повторы слов — частичные проходы (указание пользователя 2026-09-08)

Пользователь: «почему нельзя ориентироваться на распознавание и убирать именно опечатки? „die Erde, die Erde“ — повтор
композитора, в стихах его нет, можно просто несколько раз подсвечивать; то же „ihr Bild, ihr Bild“».

- **Принцип: маршрут выводится из того, что слышно** (декод двух движков), а текст — словарь для исправления
  «опечаток» декода. Так конвейер и работает; единица маршрута теперь **слово**, а не строка.
- **ПЕРЕПИСАНО правило о повторах.** Было: «повтор добавил композитор, но длиной в несколько слов — остаётся в
  тексте». Стало: **композиторский повтор слов в текст не входит; он — ещё один проход по той же строке, в
  котором интервалы стоят только у повторённых слов (`null` у остальных), и те же слова подсвечиваются ещё раз.**
  Текст остаётся стихами. Плеер это уже переживает (`buildWordIndex` пропускает не-массивы). Первое произнесение
  строки — всегда полное; частичным бывает только повтор. Следствие для D 118: удвоение «ich finde» сейчас
  вписано в текст — по новому правилу его место в маршруте; менять опубликованный текст с его переводом —
  решение редактора, не сделано.
- Детектор (`route_detect.py`): короткие возвраты на 1–6 слов (штраф 1.5 + 0.3 за слово), в том числе внутри
  фразы. На D 911/4 нашёл повтор трёх последних слов у 4 записей из 5 и повтор в конце 2-й строфы у двух.
  Ограничение: на слабом декоде он шумит (у Маттеи в Rückblick породил ложный повтор в финале), поэтому
  частичные повторы принимаются по консенсусу записей или по слуху пользователя, а не по одной записи.

### Проверки пользователя 2026-09-08 и указание «не изобретать велосипед»

- Маттеи, Rückblick, финал — на слух **нормально**: частичный повтор, найденный словным детектором, был ложным;
  подтверждено правило — частичные повторы принимаются по консенсусу записей или по слуху, не по одной записи.
- Хоттер, Rückblick, последние «stille stehn» — поёт позже и медленно (147.8–152.8 с), а разметка сжала их в конец
  предыдущей фразы. Причина: фильтр «фраз без букв» выбросил тихие медленные фразы конца, где декод дал 1–2 буквы.
  Исправлено точечно в файле Хоттера. Урок: **фразы после последнего слова текста с голосом и хотя бы одной буквой
  декода — вокал**, их нельзя отбрасывать; последним словам песни разрешено уходить в такие фразы.
- Указание пользователя: «мы изобретаем велосипед; проблемы „что в каком моменте поётся“ решали — прочти, как это
  делали, и исправь». Стандартная схема в литературе (MIREX Lyrics-to-Audio Alignment; Whisper-based SongTrans 2024,
  STARS 2025; Viterbi forced alignment по постериограммам; MFA): **разделение источников → распознавание пения
  настоящим ASR → сопоставление распознанного с текстом (повторы, варианты) → принудительное выравнивание для
  таймингов**. Наш конвейер повторяет архитектуру, но распознавателем служил жадный CTC-декод речевых моделей —
  слабое звено. Решение: распознаватель — Whisper (large-v3) по вокальному стему, с сегментными и пословными
  таймкодами; CTC-выравнивание в окнах остаётся для точного «когда». Проверяется на D 911/4.

## Делаем как в литературе: распознаватель — Whisper (указание пользователя 2026-09-08)

Пользователь: «erfroren — подсветка не на месте, „die Erde, die Erde“ было плохо, „ihr Bild, ihr Bild“ было плохо.
Мы можем сделать так, как делают грамотные люди?» — Да. ПЕРЕПИСАНО звено распознавания: было «маршрут и варианты
по жадному CTC-декоду речевых моделей (по фразам)» → стало «**распознаватель — Whisper large-v3 по вокальному стему**
(слова с таймкодами); распознанные слова сопоставляются со словами стихотворения ДП с повторами (возвраты) и
подстановками (варианты — берётся распознанное слово); окна выравнивания — по таймкодам Whisper; CTC-выравнивание
двух движков внутри окон — **буквами реально спетого слова** (для варианта — его буквами, не текста); дальше —
прежние атаки, концы, минимальная длительность, частичные проходы». Приёмка на D 911/4 (Quasthoff): три места,
названные пользователем — тайминг «erfroren», повтор «die Erde», повтор «ihr Bild».
Результат на всех пяти записях (2026-09-08): маршруты совпали в структуре (каждая строфа дважды; третья —
целиком, строки 3–4, строки 1–2; повторы слов «die Erde» дважды и «ihr Bild dahin» трижды, у Хоттера дважды),
единственный подтверждённый вариант — «erfroren» у Quasthoff. Конвейер `wh_pipeline.py` — действующий стандарт;
Whisper на CPU — 3–7 минут на запись. Уроки: неуслышанное Whisper'ом слово — пропуск (дёшево), не подстановка;
частичный проход — только повтор (возврат назад), первое произнесение строки всегда полное; вариант — целое слово,
не склейка соседей, и оба движка слышат его ближе к версии Whisper с запасом.
Результат на Quasthoff (2026-09-08): Whisper large-v3 (CPU, 7 мин на запись) — 204 слова, уверенность 0.95; в транскрипте
прямо стоят «die Erde, die Erde» в обоих произнесениях строки, «erfroren» в обоих проходах, «ihr Bild, ihr Bild dahin»
дважды и «ihr Bild dahin» в конце. Конвейер `planning/audio/scripts/wh_pipeline.py`: частичные проходы для повторов
слов; вариант erfroren подтверждён обоими CTC-движками в двух местах, ложные варианты Whisper (dritten/dringend/alten)
отсеяны той же проверкой; изолированное плохо услышанное слово-прыжок считается вставкой, не повтором.

### Без фантомных слов; спетый вариант показывается (указание пользователя 2026-09-08)

Пользователь: «ihr Bild dahin, ihr Bild dahin — в одном из мест ты даёшь лишний dahin, которого там реально нет; есть
ещё аналогичные ошибки. И сделай наконец подсветку erfroren там, где это erfroren».
- ПЕРЕПИСАНО: было «первое произнесение строки всегда полное; неспетые слова получают нулевой интервал у соседа».
  Стало: **неспетое в проходе слово — `null`**, в любом проходе. Певец может разорвать строку («…ihr Bild,» —
  и лишь потом «ihr Bild dahin»; «…die Erde,» — «die Erde seh'»); нулевой интервал плеер склейкой пауз растягивал
  до следующего слова — так и возникал фантомный «dahin». Транскрипту Whisper в таких разрывах верим.
- Спетый вариант показывается в строке вместо слова текста, пока выбрана эта запись (`word-sync.md`, поле
  `variants`; `LineDe.vue` / `SongView.vue`). Текст песни не меняется.

## Контрольная песня — случайная и короткая (указание пользователя 2026-09-08)

После D 911/4 («в целом норм, но я не всё проверил») — приёмка конвейера на **случайной** песне корпуса, о которой
заранее ничего не известно, не очень длинной. Выбор воспроизводимый (зерно 20260908) среди песен с опубликованным
текстом, ≥3 записями и ≤80 словами (111 кандидатов): **D 911/15 «Die Krähe»** (12 строк, 55 слов, 5 записей той же
пятёрки исполнителей). Конвейер — действующий стандарт `wh_pipeline.py` без ручных правок; по результату судится,
насколько получилось.

### Итог контрольной песни D 911/15 (2026-09-08)

Полностью автоматически чисто прошли 2 записи из 5 (Quasthoff, Маттеи: маршрут совпал, вариантов нет, ложные
варианты Whisper отсеяны). У трёх записей 1942–1962 гг. Whisper по вокальному стему **потерял строки** (первые
полторы у Андерса, последнюю у Фишер-Дискау, финальный повтор у Хоттера) — распознаватель ненадёжен на шумных
стемах, и неуслышанные слова остались бы без подсветки. Закрыто **запасным путём**: маршрут-консенсус записей одной
песни (тот же текст и партитура) + CTC-выравнивание с привязкой по декоду (`fallback.sh`), стоимость привязки
0.27–0.31/букву. Правило: если у записи Whisper даёт заметно меньше слов, чем у остальных записей песни, или её
маршрут — строгое подмножество консенсусного, запись идёт запасным путём; отклонения одной записи от консенсуса —
кандидаты в выбросы, а не новая истина. Проверено, помогает ли Whisper по миксу вместо стема на старых записях: **нет единого ответа** — Андерс 49 → 56 слов
(из 64 спетых), Фишер-Дискау 58 → 71 (с галлюцинациями во вступлении), Хоттер 61 → 45 (хуже); на миксе появляются
ложные сегменты на фортепианном вступлении. Ни стем, ни микс не надёжнее другого на архивных записях. Действующее
решение — как сделано: Whisper по стему там, где его маршрут полон, иначе запасной путь по консенсусу. Возможное
улучшение (не сделано): гонять Whisper и по стему, и по миксу и брать транскрипт, лучше сопоставляющийся с текстом.

## Вторая контрольная — D 688/1 «Non t'accostar all'Urna» (2026-09-08)

Die Krähe условно принята («то, что я послушал, было норм»). Второй розыгрыш того же зерна дал **итальянскую** песню
(Метастазио; 18 строк, 70 слов, 4 записи 1982–2005: Popp, Augér, Bartoli, Brownlee) — проверка общности конвейера
по языку. Следствия, сделанные сразу: язык песни — параметр спецификации (`lang`): Whisper получает его явно, второй
CTC-движок берётся по языку (`jonatasgrosman/wav2vec2-large-xlsr-53-<lang>`), нормализация текста складывает
диакритику (à→a, é→e …) для сопоставления и словарей движков. Выбор запасного пути — автоматический: запись идёт
по консенсусу, если её маршрут теряет строки, которые есть у эталонной записи, или Whisper дал у неё <90 % слов эталона.

### Итог второй контрольной D 688/1 (2026-09-08): язык обобщён, сопрано — слабое место

- Языковая обобщённость сработала: `lang`/`ctc_model` в спецификации, Whisper с языком, складывание диакритики.
  Бартоли и Браунли — полный маршрут автоматически (18/18 строк), вариантов нет.
- **Перестановка соседних слов у Whisper — не вариант**: слово, совпадающее с соседом в ±2 позициях, отсеивается
  (Браунли: «tuoi pianti» ↔ «pianti tuoi»).
- **Сопрано — слабое место обоих распознавателей**: у Оже Whisper дал 40 слов из 70, у Попп потерял строку;
  автоматический запасной путь сработал, но привязка по CTC-декоду не принята (1.2–1.5/букву) — итальянская
  CTC-модель на сопрано декодирует плохо, осталась грубая привязка. Для D 118 (Сэмпсон, сопрано) когда-то помогли
  окна по энергии и ручная сверка; для корпуса нужен третий признак для сопрано — TODO, после слуха пользователя.
- Ошибка «начало слова за концом записи» (пустой срез огибающей) — исправлена во всех конвейерах.

### Галлюцинации Whisper на живых записях (замечание пользователя 2026-09-08, Браунли D 688/1)

В конце живой записи Whisper «услышал» на аплодисментах «Grazie a tutti» через 20 с после конца пения, а сопоставление
приняло эти слова за конец текста — последние два слова строки уехали на 191-ю секунду. Правило (после проверки: признак «нет букв у движков» не сработал —
на аплодисментах CTC-модель тоже «слышит буквы»): **распознавателю верим только внутри голосовых фраз по энергии
стема** — слово Whisper вне фраз (допуск 0.6 с) и дальше 3 с от предыдущего принятого отбрасывается до сопоставления;
конец пения — конец последней голосовой фразы (+0.5 с), позже него слов нет; строки текста, оставшиеся без якорей
после отсева (до 8 слов), дописываются и выравниваются CTC внутри конца пения. Урок шире: распознаватель верить можно только там, где есть голос по
акустике; всё, что вне пения, — шум для всех стадий.

### Общая ошибка: последнее слово песни не тянулось до конца пения (замечание пользователя 2026-09-08)

Пользователь: «„la dormir“ слишком быстро прошло… никаких аплодисментов у Браунли нет. Нам нужны общие решения,
а не ад-хок». Поправка к предыдущей записи: «Grazie a tutti» у Браунли и Бартоли — галлюцинация Whisper на
**тишине в конце файла**, не аплодисменты; отбрасывать её было верно, но причина претензии другая и общая.
Во всех конвейерах для последнего слова песни «начало следующего слова» подменялось значением «конец + 0.5 с», и
продление до смолкания голоса для него не работало никогда: певец держит финальную ноту, а подсветка уже ушла
(у Браунли слово кончалось на 169.4 при голосе до 171.6; у Хоттера в Rückblick это же место чинилось руками).
Исправлено в `wh_pipeline.py` и `pipeline.py`: предел продления последнего слова — конец пения (конец последней
голосовой фразы). Пересобраны непринятые песни D 911/4, D 911/15, D 688/1 (Браунли: 168.7–171.7). Принятые
D 118 и D 911/8 не тронуты — их финальные слова удлинятся тем же исправлением, если пользователь захочет.

### ПЕРЕПИСАНО 2026-09-08: заморожено всё, что уже обсуждалось

Пользователь: «не надо пересобирать то, о чём мы уже говорили! Ты можешь испортить! Я не собираюсь их ещё раз
переслушивать. Тесты на прошлых песнях делать можно, но если хочешь поменять что-то в обсуждённых песнях — говори
что именно, и перед этим я послушаю, верно ли это место сейчас; если верно — менять нечего».
Было: «принятая пользователем разметка не пересчитывается; остальное можно». Стало: **любая песня, которую пользователь
уже слушал и обсуждал, заморожена целиком** (сейчас: D 118, D 911/8, D 911/4, D 911/15, D 688/1). Общие исправления
конвейера применяются к **новым** песням; на замороженных их можно только *прогонять как тест* без записи в
`app/`. Если исправление по расчёту меняет обсуждённую песню — назвать пользователю точные места (запись, слово,
время, было → станет) и ждать: он послушает текущее состояние, и если оно верно, менять нечего.
Инцидент: пересборка D 911/4 и D 911/15 после исправления «последнее слово до конца пения» откачена
(файлы возвращены к `34a96fe`); менялся только конец последнего слова каждой записи — список в чате.

### «Странно, что это касается только последнего слова» — не только (2026-09-08, D 688/1)

Пользователь усомнился, и проверка голосовыми дырами (голос ≥45 % опорной дольше 1 с без единого слова) вскрыла
четыре общих дефекта; все исправлены на стадиях конвейера, а не в файлах:
- **Неуслышанные распознавателем слова заполнялись только до трёх подряд.** У Бартоли Whisper услышал в двух строках
  лишь первое и последнее слово — пять слов между ними стали `null`, 12 с пения без подсветки. Стало: певец слов не
  пропускает — все пропущенные между двумя услышанными заполняются (без якоря); `null` остаётся только для разрыва
  строки, когда те же слова поются в следующем проходе той же строки (Erstarrung: «…ihr Bild, | ihr Bild dahin»).
- **Окно выравнивания группы строилось по её собственным якорям** (±0.5 с) — заполненные слова втискивались в две
  секунды. Стало: границы окна — соседние якоря других групп; вес якоря — по уверенности Whisper.
- **Продление конца слова** обрывалось на первом вдохе (0.12 с тишины), не работало у последнего слова песни, а в
  запасном пути ограничивалось фразой из грубой привязки и списком фраз, из которого фильтр «без букв» вычеркнул
  бессловесные мелизмы. Стало единообразно в обоих конвейерах: конец слова тянется до конца **цепочки голосовых
  фраз** (по полному списку фраз), разделённых менее чем 1 с — порог склейки плеера, — не дальше начала следующего
  слова и конца пения.
- **Признак «слово без букв»** (для отдельно стоящего тире) был регулярным выражением по латинице и умляутам —
  итальянское «è» считалось словом без букв и получало нулевой интервал у следующего слова. Стало: по сложенным
  буквам / Unicode-категории. Урок: любое языковое допущение проверять на не-немецкой песне.
- **QA-ворота**: `holes.py` считает голосовые дыры по каждой записи; пока их не 0, файл не выкладывается.
  D 688/1 после исправлений: 0 дыр у всех четырёх. Замороженные песни не пересобирались; из этих исправлений их
  коснулось бы только продление концов (список мест — в чате 2026-09-08).

## Третья контрольная — 911/13 «Die Post» (2026-09-08)

По просьбе пользователя («нужна ещё одна песня для теста») — третий розыгрыш того же зерна: **D 911/13**, язык `de`.
Итог: пять файлов выложены, голосовых дыр 0/5; четыре записи (Quasthoff, Fischer-Dieskau, Anders, Hotter) сошлись на одном
маршруте из 32 проходов каждая своим Whisper-маршрутом, Маттеи дополнен по консенсусу. Справка —
`planning/research/d911-13-die-post-route.md`, очереди — `planning/audio/queues-d911-13.md`.

Песня вскрыла пять дефектов конвейера (фантомные повторы Whisper в проигрышах; слитые слова; рефрен, объявленный
вставками; плохие совпадения дешевле структуры; потерянная первая фраза после вступления). Все исправлены **общими
правилами** в `wh_pipeline.py`, проверены на пяти записях и тестом на замороженных песнях:

- **Буквенная масса CTC** — акустический бюджет: Σ(1 − P(бланк)) по кадрам интервала ≈ число букв, которые модель
  слышит там (фортепиано даёт бланки); максимум по двум движкам. У настоящих проходов масса/буквы ≥ ~1.0 (минимум по
  пяти записям 0.64 — тихий финал Маттеи), у фантомов ≤ 0.14. Порог `MASS_MIN = 0.4`.
- **Фантомные проходы Whisper.** В проигрышах стем хранит эхо фортепиано, Whisper дописывает «Mein Herz, mein Herz» со
  слипшимися таймкодами (`condition_on_previous_text=False` стояло — это звук, не эхо текста). Окно прохода — от его
  первого якоря до первого якоря следующего прохода минус 0.25 с; слипшиеся таймкоды → пустое окно. Проход с массой
  < 0.4·букв отбрасывается. Старое правило «эталон — запись с наибольшим числом проходов» **отменено**: оно выбирало
  запись с фантомами (у Андерса 6 «mein Herz» вместо 3), и остальные наследовали её маршрут.
- **Заполнение пропусков** (Whisper не услышал ↔ певец не спел): < 3 слов — заполняем всегда (певец слов не пропускает,
  купюра — это целые строки); иначе по бюджету между якорями: сначала хвост строки перед пропуском, потом голова строки
  после, потом целые строки между. Голова текста до первого услышанного слова — по бюджету от начала записи (энергетические
  фразы пропускают тихое начало); хвост — до `END_SING`. Исключение «разрыв строки с повтором» снято: оно блокировало
  заполнение «liebes Liebchen» во всех пяти записях.
- **Сопоставление (ДП)**: слияние — одно слово Whisper = два соседних слова текста («Liebesliebchen», d ≤ 0.25, якорь
  делится по буквам), разбиение — два слова Whisper = одно слово текста («in sera» = «rinserra»); прыжок к **началу любой
  строки** вперёд или назад стоит как повтор `1.2 + 0.35·строк` — перестановка строк композитором законна («…für dich,
  mein Herz, mein Herz, was drängst…»); внутрь строки вперёд — 0.8/слово при ≤ 2, дальше дорого. Цена совпадения
  выпуклая `d + d²`, но не выше 1.6: слово на своём месте между соседями дешевле вставки + пропуска (≥ 1.7) даже при
  d = 1 («Ma» = «Ah» у Браунли). Вставка дорогая, если такое слово есть в тексте **рядом** (±2 строки):
  `0.9 + 1.5·(1 − d_loc)`; для мусора — 0.9. Без этого четыре точных «mein Herz, mein Herz» были дешевле как вставки.
- **Консенсус записей по строфам** (`consensus.py`): голосуют записи, у которых в строфе есть её первая строка;
  большинство, при равенстве — запись с наибольшим числом якорей Whisper. Решение по записи: маршрут совпал — **свой**;
  свой маршрут — подпоследовательность консенсуса и якорей ≥ 60 % — **починка** (`CONSENSUS=route.json`: недостающие
  проходы вставляются без якорей после своего предшественника при самом раннем вложении, если между соседними якорями
  хватает буквенной массы; лишние проходы записи не трогаются, о них сообщается); иначе — **запасной путь** с
  консенсусным маршрутом. После сборки — ворота дыр; запись с дырами уходит на запасной путь.
- **Варианты**: декод для проверки — по собственному спетому отрезку слова (`raw_end`), не по продлённому концу; иначе
  вторая «erfroren» у Кваст­хоффа терялась.
- **VAD Whisper** (`WH_VAD=1` в `whisper_tr.py`): чинит первую фразу и фантомы у Ф.-Дискау и Хоттера, но срезает тихий
  финал Маттеи (20 с) — по умолчанию выключен; время то же (90–100 с на запись).

Тест без записи в `app/` — `test_run.sh` (`OUT_DIR`): маршрут, сдвиги начал > 0.3 с по общим проходам и варианты против
файла сайта. Замороженные песни им проверены и **не пересобирались**: D 911/15 — без изменений; D 911/4 — у четырёх
записей дописано «Gras?» в конце последнего «Wo find ich grünes Gras?», один сдвиг (`U5flDePTl6g` 1:1/1 52.66→53.32),
варианты те же; D 688/1 — 6 сдвигов у `DaIxqTHJlXg`, один у `NiL8E3PzeVs` (список — в справке).

Полный прогон новой песни: `run_control.sh` (скачать → Demucs → эмиссии → Whisper → `finish_control.sh`: свои маршруты →
консенсус → решения → файлы сайта → ворота с откатом на запасной путь). Служебное поле файла таймингов `anchored`
(число слов с якорем Whisper) — для консенсуса; плеер его не читает.

TODO: ворота не видят голос до первого слова (у Маттеи 11.1–12.8 с поётся без подсветки — тихое начало, Whisper его не
слышит, CTC сжимает); починка при неоднозначном вложении (пробовать позднее вложение, если раннее не проходит по массе).

## Гигиена

Скачанный звук, вокальные стемы и веса моделей — **только в scratchpad**, никогда в репозиторий.
В репозиторий попадают лишь JSON с таймингами, привязанные к конкретному `videoId`
(тайминги действительны только для той записи, по которой считались). Сайт по-прежнему проигрывает
официальный ютюб-эмбед; скачивание — исключительно промежуточный технический шаг ради таймкодов.

Действующая редакция: 2026-09-07 (в тот же день переписана дважды: раздел о повторах и выбор
основного движка/арбитраж). Рабочий конвейер — `planning/audio/scripts/` (README там же).
Формат хранения таймингов (`app/src/data/timings/`) и поведение плеера — `word-sync.md`.

## Переход к продакшену (указание пользователя 2026-09-08)

Пользователь: «я хочу переходить на продакшен. сделаем сразу 20 песен. наверное, надо купить gpu … чтобы ты сам смог это
gpu запускать отсюда … я хочу быстро сделать все песни». Решения: (1) следующая партия — **20 песен разом** через
отлаженный конвейер, без ручной отладки по одной; (2) тяжёлые стадии (Demucs, эмиссии, Whisper) — на **арендованном GPU**,
которым конвейер управляет сам из этого окружения (выбор провайдера и порядок подключения — ниже, после проверки в сети);
(3) скачивание с YouTube остаётся локальным (датацентровые IP YouTube блокирует), на GPU уходит только сжатый звук;
(4) ожидание пользователя: в каких-то песнях немецкий текст не совпадёт с тем, что поют, — конвейер обязан это
**обнаруживать и показывать** (варианты слов через `variants`; купюры и лишние слова — в отчёте по песне), а не молча
подгонять текст.

### GPU в облаке — выбор и порядок (2026-09-08, после проверки в сети)

Выбран **Modal** (serverless GPU, https://modal.com): управляется из этого окружения Python-SDK/CLI без SSH и без своих
серверов, оплата посекундная, на бесплатном плане Starter — $30 кредитов в месяц без карты, до 10 GPU одновременно.
Цены: T4 $0.59/ч, L4 $0.80/ч, A10G $1.10/ч. Оценка: запись ~1 мин GPU (Demucs ~10 с, эмиссии ~10 с, Whisper large-v3
fp16 ~20–30 с) → 100 записей (20 песен) ≈ $2 и ~15 мин при 10 параллельных контейнерах; все 2433 записи каталога
≈ 40 GPU-часов ≈ $45. Альтернативы (RunPod, Vast.ai, Lambda) дешевле за час на потребительских картах, но требуют
поднимать под и ходить по SSH — для «запускать самому отсюда» хуже.

Что нужно от пользователя один раз: аккаунт Modal (вход через GitHub) и токен — в этой сессии `! modal token new`
(откроет ссылку для подтверждения в браузере) либо создать токен в настройках Modal и выполнить
`modal token set --token-id ak-… --token-secret as-…`. Токен хранится в `~/.modal.toml`; в репозиторий не попадает.

Разделение труда: скачивание — локально (`yt-dlp`, датацентровые IP YouTube блокирует: «Sign in to confirm you're not
a bot»); на GPU уходит сжатый звук (3–5 МБ на запись), обратно — стем 16 кГц, две эмиссии и транскрипт (~10 МБ);
сопоставление, консенсус, выравнивание и ворота — локально на CPU (секунды). Скрипты: `gpu_stage.py` (Modal-приложение,
формат выходов побайтно как у `emit.py`/`emit_hf.py`/`whisper_tr.py`), `batch_gpu.sh <D…>` (партия: спецификации →
скачивание → GPU по языкам → `finish_control.sh` по песне → `batch_report.py`), `make_spec.py` (каталог песни; язык —
`planning/audio/lang-overrides.json`, 13 итальянских песен на Метастазио), раскладка `$SP/songs/<prefix>/`.

Первая партия (предложение): оставшиеся **20 песен «Зимнего пути»** (сделаны 4, 8, 13, 15) — ровно 20, один поэт,
один язык, цикл на сайте станет целиком с подсветкой. Сводка партии показывает по каждой песне: решения по записям
(свой/починка/запасной), дыры, варианты, купюры и вставки — последние два признака означают «поют не тот текст»
и требуют взгляда человека.

Критерий выбора GPU (вопрос пользователя «это самое дешёвое и удобное?», 2026-09-08): не цена за час, а **полная стоимость
именно нашей задачи** (~40–60 GPU-часов на весь каталог, всплесками) плюс возможность запускать из этой сессии без
администрирования. По этому критерию Modal на T4/L4 (бесплатные $30/мес) ≈ $0; Vast/RunPod дешевле за час, но требуют
подов и SSH; Kaggle (30 ч/нед бесплатно) автоматизируется через CLI, но громоздко и ненадёжно; Colab не автоматизируется;
покупка карты невыгодна при таком объёме.

### Перед партией — бенчмарк GPU (указание пользователя 2026-09-08, совет из ChatGPT)

Токен Modal подключён (профиль `evgobmm`, `~/.modal.toml`). **Не запускать все 20 песен сразу.** Сначала бенчмарк на одной
типичной записи: по одному прогону на T4 и на L4 с одинаковым входом и полным production-конвейером; сравнить wall-clock,
фактическую стоимость Modal, результаты выравнивания и итоговые файлы; холодный старт учитывать отдельно; регион не
форсировать. По итогам назвать GPU, который дешевле именно в $/запись, оценить стоимость всех 2433 записей и только
при корректных результатах запускать оставшиеся 20 песен Winterreise на выбранном GPU.

Указание пользователя (2026-09-08): «подумай, почитай ещё, и сделай грамотно». Перед платными внешними вычислениями —
прочитать документацию провайдера (жизненный цикл контейнера, что тарифицируется, холодный старт) и спроектировать по ней:
модели загружаются один раз на контейнер (`@modal.enter`), а не на каждый вызов; окно простоя минимальное; бенчмарк
измеряет холодный и тёплый вызов отдельно.

#### Итоги бенчмарка (2026-09-08, две записи D 911/13 — Кваст­хофф 140 с, Хоттер 151 с; полный конвейер)

| | T4 | L4 | L4 + детерминированный Whisper (`WH_TEMP0=1`) |
|---|---|---|---|
| холодный старт (4 модели) | 73 с (первый запуск — с закачкой весов в том) | 28 с | 17 с |
| тёплый вызов, с | 21.6 / 20.0 | 16.1 / 11.9 | 12.8 / 10.9 |
| $/запись | ≈ 0.0034 | ≈ 0.0031 | ≈ 0.0026 |

Сборка образа — 3.5 мин один раз. Стемы GPU/CPU идентичны (корреляция огибающих 0.9999), эмиссии совпадают в 99 % кадров.
**Выбран L4 с детерминированным декодом Whisper** (temperature 0, без отката на сэмплирование: на L4 откат породил
галлюцинацию «Untertitelung des ZDF» на 11.9–27.4 с у Хоттера и сломал начало). Оценка на все 2433 записи: ≈ 8–9 GPU-часов
≈ $8–9 (в пределах бесплатных $30/мес). Первая партия — 20 песен Winterreise.

Бенчмарк вскрыл и исправил (общие правила, `wh_pipeline.py`, `holes.py`):
- окно выравнивания группы, начинающейся словами без якоря (голова текста, вставка по консенсусу), начиналось за 0.5 с до
  первого якоря — слова зажимались в конец окна (первая строка Хоттера на 16.1 с вместо 13.3). Стало: окно от предыдущего
  якоря (или от начала записи);
- буквенная масса считается **только по языковому движку** (wav2vec2-xlsr): у MMS_FA на фортепиано размытая небланковая
  масса без букв (1.5–5 на полсекунды), раздувавшая бюджет; там, где языковой движок глух к тихому легато (Бартоли, Попп),
  свидетельством служит энергия голоса (≥ 0.5 опорной при ≥ 0.1 с на букву). Фильтр галлюцинаций тоже смотрит на буквы под
  словом: тихая атака «willst» у Андерса больше не выбрасывается;
- слабые проходы Whisper (p < 0.35 или слипшиеся таймкоды) требуют массы ≥ 0.8·букв; окно прохода без последних 30 %;
- ворота: «растянутое слово» (под словом букв в 3 раза больше, чем оно может дать, при длительности > 1 с) и пение до первого
  размеченного слова. Whisper ставит начала слов после паузы раньше, чем CTC; проверка показала, что прав CTC (энергия
  стема между якорем и буквами ≈ 0.1 опорной) — правило «начало по якорю» не введено.

### Первая партия — 20 песен Winterreise на L4 (2026-09-08)

Время: скачивание 100 записей в 4 потока — 1.7 мин; GPU (10 контейнеров L4) — 3.5 мин (вверх 284 МБ, вниз 858 МБ); сборка на
CPU в 6 потоков — 5.5 мин; итого **11 минут** (первый запуск шёл последовательно и был остановлен, см. `workflow.md`).
Ворота (0 дыр во всех записях) прошли **7 песен** — D 911/9, 10, 11, 14, 17, 18, 22 (35 записей выложены; очереди —
`planning/audio/queues-winterreise-1.md`, сводка — `report-winterreise-1.md`). 13 песен не выложены: после запасного пути
остаются дыры (55 на 65 записей). Разбор дыр: значительная часть — «растянутое слово» на долгих нотах медленных песен
(масса языкового движка на тянущейся гласной велика — порог `3·букв + 4` срабатывает ложно; пересчитать по числу
свёрнутых букв жадного декода), «пение до первого слова» у D 911/5 (голова текста не дописана), классические дыры у
запасного пути (он не пользуется буквенным бюджетом). Политика «дыры → запасной путь» не улучшает результат: у большинства
записей запасной путь даёт столько же дыр — брать лучший из двух и не публиковать при дырах. Исправлено в этой партии:
слова без букв (тире в текстах Winterreise) исключены из ворот и голосования; падение обратного хода ДП при несошедшемся
порядке (D 911/2). TODO следующей итерации: калибровка ворот по свёрнутым буквам; выбор лучшего из свой/запасной;
голова текста у запасного пути.

### Пропущенный вариант «Straßen» (вопрос пользователя 2026-09-08) и второй проход распознавания

Пользователь: «квастхоф поет здесь штрасен, почему ты это не поймал?? Weiser stehen auf den Straßen/Wegen». Разбор (D 911/20,
Кваст­хофф, 122–130 с): Whisper на всей записи выдал вместо строки титр **«Untertitelung des ZDF»** (известная галлюцинация
на немецком звуке с музыкой), слова строки были дописаны без распознавания — варианту было не с чем сравниться, хотя языковой
CTC-движок читал там `weiser steh rauf den stra ssen weisen`. Исправлено общими правилами (`wh_pipeline.py`):
- **служебные галлюцинации** (титры, «Vielen Dank», Amara.org — по языкам) отбрасываются, если меньше половины слов такого
  сегмента ложится на текст; иначе титр становился «якорем» строки;
- **второй проход**: промежутки между якорями, где есть буквы CTC, а слов Whisper нет (потерянная фраза, титр), распознаются
  заново локальным faster-whisper по плотной вырезке стема (окно по буквам, длинные — кусками ~8–13 с с разрезом по паузе;
  окно с запасом фортепиано снова даёт титр); новые слова добавляются к транскрипту, и сопоставление повторяется целиком.
  На Кваст­хоффе — 6 окон, 23 слова, вариант «Wegen, → Straßen,» найден и подтверждён;
- **подтверждение вариантов — только языковым движком**: MMS_FA — выравниватель, его жадный декод пуст, требование «оба
  движка» отвергало настоящие варианты (поэтому в партии их почти не было);
- ворота: «растянутое слово» — по числу свёрнутых букв жадного декода под словом (`> букв + 6`), а не по массе: масса на
  долгой ноте растёт, буквы — нет (три записи Gute Nacht: две с настоящей ошибкой — слова после «hören»/«zu» опаздывали,
  одна ложная — долгая нота «mit»);
- публикация **по записям**: запись выкладывается, если у неё 0 дыр, независимо от соседей по песне.
Пользователь смотрел сайт: «вроде нормально».

Стоимость облака (вопрос пользователя «а это будет дорого?», 2026-09-08): по тарифам Modal CPU $0.047/ядро-час,
память $0.008/ГиБ-час → сборка партии из 20 песен ≈ $0.10–0.15, весь каталог ≈ $3; GPU-стадия ≈ $0.003/запись → каталог
≈ $8–9. Всё в облаке для 618 песен ≈ $12–15 при бесплатных $30/мес — в пределах бесплатного. Точный расход — в Modal →
Settings → Usage & Billing (недоступно из сессии; за 2026-09-08 оценка ≈ $1).

### Итог партии 1 после второго прохода (2026-09-08, вечер)

Выложено **81 из 100 записей** (0 дыр у каждой); удержано 19 (список — `planning/audio/report-winterreise-1.md`). Второй
проход: 142 окна у 56 записей, 666 новых слов за 2 мин GPU. **Варианты на сайте — только сильные или подтверждённые**:
из 13 кандидатов на сайт прошёл один («Wegen → Straßen» у Кваст­хоффа); 12 ослышек распознавателя («Dach → Bach»,
«dringt → trinkt», «Schnee → Schnitzer»…) — в очереди на прослушивание (`variant_candidates`), подтверждённые пользователем
на слух записываются в `planning/audio/variants-confirmed.json` (сейчас — «erfroren» D 911/4) и проходят всегда.
Открытые дефекты: слова после долгой ноты ставятся с опозданием («…nicht hören — sacht, sacht»: Кваст­хофф и Маттеи в
Gute Nacht удержаны именно за это); у D 911/5 голова текста не дописывается (пение до первого слова). Сборка на CPU этой
машины — узкое место (7,8 ГБ, ≤ 3 процесса, 2 прохода по 3–7 мин) — переносится в облако (`workflow.md`).

### Порог публикации ПЕРЕПИСАН (2026-09-08, после вопроса пользователя о Gute Nacht)

Пользователь: «как такое может быть что в гуте нахт до сих пор не все исполнения??». **Было:** запись выкладывается только
при нуле голосовых дыр; запись с дырами уходит на запасной путь и, если дыры остаются, удерживается. **Стало:** запись
выкладывается при **≤ 2 дырах** (`MAX_HOLES`, по умолчанию 2); каждая дыра (время, слово) записывается в очередь
прослушивания (`holes_<vid>.txt` → `queues-*.md`, строка «ДЫРЫ НА ПРОВЕРКУ»); свой маршрут с дырами сравнивается
с запасным путём и остаётся **лучший из двух** (меньше дыр; при равенстве — свой, у него якоря Whisper); больше двух дыр —
запись удерживается в `songs/<prefix>/held/`. Причина переписи: одна ошибка в 2–4 с лишала сайта целую пятиминутную
запись; пользователю нужны все исполнения, а места ошибок он проверяет по очереди. Дефект «слова после долгой ноты
встают с опозданием» (Gute Nacht: «hören — sacht, sacht», «Türe zu! — Schreib'») — в работу как общий.

Дефект «слова после долгой ноты встают с опозданием» (запасной путь, `pipeline.py`) — найден и исправлен 2026-09-08:
сегментация фраз по энергии давала на стаккато («sacht, sacht») фразы по 0.3 с, которые отбрасывались как «без букв
в декоде» (порог ≥ 3 букв), и слова строки уезжали в следующую фразу (Кваст­хофф: «Sacht» 282.6 вместо 280.6; Маттеи:
«Schreib'» 269.9 вместо 268.5). Стало: порог букв пропорционален длине фразы (0.3 с → 1 буква), соседние фразы с паузой
< 0.5 с сливаются. Проверено на обеих записях по декоду.

### Партия 2 — 105 песен (указание пользователя 2026-09-08: «мы можем сделать сейчас сразу 100 песен?»)

Состав: «Прекрасная мельничиха» D 795 (20), «Лебединая песня» D 957 (14), разделы 1824–1825 (30) и 1826–1827 (41) — 105 песен,
498 записей. Скачивание и GPU-стадия запущены сразу (не зависят от сборки); GPU-стадия теперь пишет выходы и в том Modal
`schubert-data` (/data/align, /data/audio) — их читает облачная сборка, которая переносится параллельно (локальная сборка
500 записей заняла бы 3–4 часа при 7,8 ГБ памяти).

### Сборка в облаке (2026-09-08, вечер) — сделано

`cpu_stage.py` (Modal, CPU 2 ядра / 4 ГиБ на песню, до 40 контейнеров): те же скрипты (`planning/audio/scripts` монтируются
как /root/sp/tools), данные сайта — копией в образе, выходы GPU-стадии — из тома `schubert-data`; два прохода как локально,
дораспознавание окон — через развёрнутый GPU-класс `Retr` (`modal deploy gpu_stage.py`). Проверка на D 911/13: 76 с на песню,
маршруты и варианты совпали с опубликованными (1 сдвиг > 0.3 с из 129 слов у одной записи). `batch_gpu.sh` теперь собирает
в облаке по умолчанию (`LOCAL_FINISH=1` — по-старому); локально остаются скачивание, сводка, очереди, коммит.

### Инцидент 2026-09-08 18:30: Modal отключил workspace («workspace … is disabled»)

Во время GPU-стадии партии 2 (488 записей на 10 L4) Modal вернул `ConflictError: workspace … is disabled`; вычисления
(GPU и CPU) недоступны, чтение тома работает. Успело: 108 записей партии 2 — в томе `schubert-data`. Расход за день по оценке
$2–3 из бесплатных $30, так что вероятнее проверка нового аккаунта (создан в тот же день, сразу тяжёлая нагрузка), чем
исчерпание кредитов. Действие — на стороне пользователя: Usage & Billing / баннер в workspace / письмо от Modal; после
восстановления GPU-стадия и сборка продолжаются идемпотентно (`batch_gpu.sh` пропускает готовое; выходы из тома можно
забрать `modal volume get`). Локально тяжёлые стадии не запускать (7,8 ГБ, 40 часов на партию).

2026-09-08 ~18:50: пользователь восстановил доступ к Modal («я исправил, давай»); партия 2 возобновлена той же цепочкой
(GPU-стадия идемпотентна — пропускает записи с готовыми выходами, затем `finish_cloud.sh`). Проба доступности —
`modal run gpu_stage.py::retr --windows empty.json` (без окон не поднимает контейнер).

### Партия 2 — итог (2026-09-08, 19:55)

105 песен (D 795 ×20, D 957 ×14, разделы 1824–1825 и 1826–1827), 498 записей: **433 выложены** (≤ 2 дыр), 52 удержаны
(> 2 дыр), сводка и очереди — `planning/audio/report-batch2.md`, `queues-batch2.md`. Время после восстановления Modal:
GPU-стадия ~15 мин (с повторами), сборка в облаке 105 песен — ~10 мин (99 песен за 6 мин; клиент упал на асинхронной ошибке
Modal перед последними 6 — досбраны отдельным вызовом; повторяющаяся ошибка `aclose(): asynchronous generator is already
running` у клиента modal 1.5.5 — безвредна для данных, но обрывает поток результатов: добавить повтор на недособранные песни).
Вариантов на сайт прошло 42 (правило «сильный / вторая запись / подтверждённый»); правило «вторая запись» пропускает
одинаковые ослышки разных певцов («Zither → Zittern» в трёх записях) — кандидаты и принятые варианты требуют слуха
пользователя; 46 песен с пометкой «много вставок» — признак несовпадения текста или шумного распознавания.

Дополнение к порогу публикации (2026-09-08, 20:05, после проверки пяти случайных песен партии 2): запись **удерживается также
при любой дыре длиннее 8 с** (`MAX_HOLE_SEC`) — «растянутое слово» на 30 с у Шрайера в «Im Walde» означало целую строфу под
одним словом и проходило по числу дыр (2). Применено к партии 2 по уже посчитанным дырам без пересчёта.

### Решение по записи ПЕРЕПИСАНО (2026-09-08, 20:20, разбор «Im Walde» D 834)

**Было:** запись, чей маршрут расходится с консенсусом «иной структурой» (лишние/другие проходы), уходит на запасной путь
с консенсусным маршрутом. **Стало:** при доле якорей Whisper ≥ 60 % запись оставляет **свой** маршрут, починка лишь
добавляет недостающие проходы там, где под них есть буквы; запасной путь — только при якорях < 60 % или по воротам.
Причина: певцы повторяют строки по-разному (Герхаэр в «Im Walde» поёт «Und doch sie nimmer abgepflückt» дважды, Шрайер
и Гёрне — полный повтор двух строк), Whisper повторы схлопывает, и «большинство» навязывало всем короткий маршрут —
у трёх записей строфа под одним словом на 20–30 с. Тексты с повторами, записанными в строки (1:8–1:10 в D 834), для
сопоставления естественны — ДП идёт по ним последовательно.
